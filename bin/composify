#!/usr/bin/env bash

if [ ! -f composer.phar ]; then
    curl -s https://getcomposer.org/installer | php
fi

if [[ "$3" != "--no-update" ]]; then
    php composer.phar self-update
fi

php composer.phar require $1:$2 $3 $4
